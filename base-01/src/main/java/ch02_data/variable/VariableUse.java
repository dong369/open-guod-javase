package ch02_data.variable;

/**
 * The class/interface
 * 变量：在程序执行的过程中，在某个范围内其值可以发生改变的量。从本质上讲，变量其实是内存中的一小块区域
 * 成员变量：方法外部/类内部定义的变量，整个类有效，存储在堆内存，随对象消失，有默认值。
 * 局部变量：方法中的变量，仅某个范围有效（一般指的就是方法, 语句体内），存储在栈内存中，执行结束后自动释放内存，无默认值。
 *
 * @author guodd
 * @version 1.0 use jdk 1.8
 */
public class VariableUse {
}
